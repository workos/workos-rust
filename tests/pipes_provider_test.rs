// This file is auto-generated by oagen. Do not edit.

mod common;

use wiremock::matchers::{method, path as path_matcher};
use wiremock::{Mock, MockServer, ResponseTemplate};
use workos::Error;

#[tokio::test]
async fn pipes_provider_list_organization_data_integration_configurations_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher(
            "/organizations/test_id/data_integration_configurations",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/data_integration_configuration_list_response.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .pipes_provider()
        .list_organization_data_integration_configurations("test_id")
        .await;
}

#[tokio::test]
async fn pipes_provider_list_organization_data_integration_configurations_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/organizations/test_id/data_integration_configurations",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .pipes_provider()
        .list_organization_data_integration_configurations("test_id")
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn pipes_provider_list_organization_data_integration_configurations_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/organizations/test_id/data_integration_configurations",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .pipes_provider()
        .list_organization_data_integration_configurations("test_id")
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn pipes_provider_list_organization_data_integration_configurations_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/organizations/test_id/data_integration_configurations",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .pipes_provider()
        .list_organization_data_integration_configurations("test_id")
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn pipes_provider_list_organization_data_integration_configurations_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("GET"))
        .and(path_matcher(
            "/organizations/test_id/data_integration_configurations",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .pipes_provider()
        .list_organization_data_integration_configurations("test_id")
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn pipes_provider_update_organization_data_integration_configuration_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/organizations/test_id/data_integration_configurations/test_id",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string(include_str!(
            "fixtures/data_integration_configuration_response.json"
        )))
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client
        .pipes_provider()
        .update_organization_data_integration_configuration(
            "test_id",
            "test_id",
            workos::pipes_provider::UpdateOrganizationDataIntegrationConfigurationParams::new(
                serde_json::from_str("{}").expect("parse stub for ConfigureDataIntegrationBody"),
            ),
        )
        .await;
}

#[tokio::test]
async fn pipes_provider_update_organization_data_integration_configuration_unauthorized() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(401).set_body_string("{\"message\":\"Unauthorized\"}");
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/organizations/test_id/data_integration_configurations/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .pipes_provider()
        .update_organization_data_integration_configuration(
            "test_id",
            "test_id",
            workos::pipes_provider::UpdateOrganizationDataIntegrationConfigurationParams::new(
                serde_json::from_str("{}").expect("parse stub for ConfigureDataIntegrationBody"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 401);
}

#[tokio::test]
async fn pipes_provider_update_organization_data_integration_configuration_not_found() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(404).set_body_string("{\"message\":\"Not found\"}");
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/organizations/test_id/data_integration_configurations/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .pipes_provider()
        .update_organization_data_integration_configuration(
            "test_id",
            "test_id",
            workos::pipes_provider::UpdateOrganizationDataIntegrationConfigurationParams::new(
                serde_json::from_str("{}").expect("parse stub for ConfigureDataIntegrationBody"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 404);
}

#[tokio::test]
async fn pipes_provider_update_organization_data_integration_configuration_rate_limited() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(429)
        .insert_header("retry-after", "1")
        .set_body_string("{\"message\":\"Slow down\"}");
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/organizations/test_id/data_integration_configurations/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .pipes_provider()
        .update_organization_data_integration_configuration(
            "test_id",
            "test_id",
            workos::pipes_provider::UpdateOrganizationDataIntegrationConfigurationParams::new(
                serde_json::from_str("{}").expect("parse stub for ConfigureDataIntegrationBody"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 429);
    assert_eq!(api.retry_after, Some(std::time::Duration::from_secs(1)));
}

#[tokio::test]
async fn pipes_provider_update_organization_data_integration_configuration_server_error() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(500).set_body_string("{\"message\":\"Internal error\"}");
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/organizations/test_id/data_integration_configurations/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .pipes_provider()
        .update_organization_data_integration_configuration(
            "test_id",
            "test_id",
            workos::pipes_provider::UpdateOrganizationDataIntegrationConfigurationParams::new(
                serde_json::from_str("{}").expect("parse stub for ConfigureDataIntegrationBody"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 500);
}

#[tokio::test]
async fn pipes_provider_update_organization_data_integration_configuration_bad_request() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(400)
        .set_body_string("{\"code\":\"validation_error\",\"message\":\"Bad request\"}");
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/organizations/test_id/data_integration_configurations/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .pipes_provider()
        .update_organization_data_integration_configuration(
            "test_id",
            "test_id",
            workos::pipes_provider::UpdateOrganizationDataIntegrationConfigurationParams::new(
                serde_json::from_str("{}").expect("parse stub for ConfigureDataIntegrationBody"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 400);
    assert_eq!(api.code.as_deref(), Some("validation_error"));
}

#[tokio::test]
async fn pipes_provider_update_organization_data_integration_configuration_unprocessable() {
    let server = MockServer::start().await;
    let template = ResponseTemplate::new(422).set_body_string("{\"message\":\"Unprocessable\"}");
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/organizations/test_id/data_integration_configurations/test_id",
        ))
        .respond_with(template)
        .expect(1)
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let err = client
        .pipes_provider()
        .update_organization_data_integration_configuration(
            "test_id",
            "test_id",
            workos::pipes_provider::UpdateOrganizationDataIntegrationConfigurationParams::new(
                serde_json::from_str("{}").expect("parse stub for ConfigureDataIntegrationBody"),
            ),
        )
        .await
        .expect_err("expected error");
    let api = match &err {
        Error::Api(api) => api.as_ref(),
        other => panic!("expected Error::Api, got {other:?}"),
    };
    assert_eq!(api.status, 422);
}
