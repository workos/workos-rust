// Code generated by oagen. DO NOT EDIT.

mod common;

use wiremock::matchers::{method, path as path_matcher};
use wiremock::{Mock, MockServer, ResponseTemplate};

#[tokio::test]
async fn authorization_check_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher(
            "/authorization/organization_memberships/test_id/check",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_list_resources_for_membership_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher(
            "/authorization/organization_memberships/test_id/resources",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_list_effective_permissions_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher(
            "/authorization/organization_memberships/test_id/resources/test_id/permissions",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_list_effective_permissions_by_external_id_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher(
            "/authorization/organization_memberships/test_id/resources/test_id/test_id/permissions",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_list_role_assignments_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher(
            "/authorization/organization_memberships/test_id/role_assignments",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_assign_role_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher(
            "/authorization/organization_memberships/test_id/role_assignments",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_remove_role_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/authorization/organization_memberships/test_id/role_assignments",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_remove_role_assignment_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/authorization/organization_memberships/test_id/role_assignments/test_id",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_list_organization_roles_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/authorization/organizations/test_id/roles"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_create_organization_role_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/authorization/organizations/test_id/roles"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_get_organization_role_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher(
            "/authorization/organizations/test_id/roles/test_id",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_update_organization_role_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PATCH"))
        .and(path_matcher(
            "/authorization/organizations/test_id/roles/test_id",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_delete_organization_role_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/authorization/organizations/test_id/roles/test_id",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_add_organization_role_permission_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher(
            "/authorization/organizations/test_id/roles/test_id/permissions",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_set_organization_role_permissions_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PUT"))
        .and(path_matcher(
            "/authorization/organizations/test_id/roles/test_id/permissions",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_remove_organization_role_permission_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/authorization/organizations/test_id/roles/test_id/permissions/test_id",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_get_resource_by_external_id_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher(
            "/authorization/organizations/test_id/resources/test_id/test_id",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_update_resource_by_external_id_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PATCH"))
        .and(path_matcher(
            "/authorization/organizations/test_id/resources/test_id/test_id",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_delete_resource_by_external_id_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("DELETE"))
        .and(path_matcher(
            "/authorization/organizations/test_id/resources/test_id/test_id",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_list_memberships_for_resource_by_external_id_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher(
            "/authorization/organizations/test_id/resources/test_id/test_id/organization_memberships",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_list_role_assignments_for_resource_by_external_id_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher(
            "/authorization/organizations/test_id/resources/test_id/test_id/role_assignments",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_list_resources_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/authorization/resources"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_create_resource_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/authorization/resources"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_get_resource_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/authorization/resources/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_update_resource_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PATCH"))
        .and(path_matcher("/authorization/resources/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_delete_resource_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("DELETE"))
        .and(path_matcher("/authorization/resources/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_list_memberships_for_resource_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher(
            "/authorization/resources/test_id/organization_memberships",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_list_role_assignments_for_resource_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher(
            "/authorization/resources/test_id/role_assignments",
        ))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_list_environment_roles_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/authorization/roles"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_create_environment_role_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/authorization/roles"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_get_environment_role_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/authorization/roles/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_update_environment_role_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PATCH"))
        .and(path_matcher("/authorization/roles/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_add_environment_role_permission_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/authorization/roles/test_id/permissions"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_set_environment_role_permissions_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PUT"))
        .and(path_matcher("/authorization/roles/test_id/permissions"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_list_permissions_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/authorization/permissions"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_create_permission_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/authorization/permissions"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_get_permission_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/authorization/permissions/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_update_permission_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PATCH"))
        .and(path_matcher("/authorization/permissions/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn authorization_delete_permission_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("DELETE"))
        .and(path_matcher("/authorization/permissions/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.authorization();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}
