// Code generated by oagen. DO NOT EDIT.

mod common;

use wiremock::matchers::{method, path as path_matcher};
use wiremock::{Mock, MockServer, ResponseTemplate};

#[tokio::test]
async fn webhooks_list_webhook_endpoints_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path_matcher("/webhook_endpoints"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.webhooks();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn webhooks_create_webhook_endpoint_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("POST"))
        .and(path_matcher("/webhook_endpoints"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.webhooks();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn webhooks_update_webhook_endpoint_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("PATCH"))
        .and(path_matcher("/webhook_endpoints/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.webhooks();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}

#[tokio::test]
async fn webhooks_delete_webhook_endpoint_round_trip() {
    let server = MockServer::start().await;
    Mock::given(method("DELETE"))
        .and(path_matcher("/webhook_endpoints/test_id"))
        .respond_with(ResponseTemplate::new(200).set_body_string("{}"))
        .mount(&server)
        .await;
    let client = common::test_client(&server).await;
    let _ = client.webhooks();
    // Smoke: client + service handle compile and resolve.
    assert!(server.uri().starts_with("http"));
}
