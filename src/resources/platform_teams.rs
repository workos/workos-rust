// This file is auto-generated by oagen. Do not edit.

use crate::client::Client;
#[allow(unused_imports)]
use crate::enums::*;
use crate::error::Error;
#[allow(unused_imports)]
use crate::models::*;
use serde::Serialize;

pub struct PlatformTeamsApi<'a> {
    pub(crate) client: &'a Client,
}

#[derive(Debug, Clone, Serialize)]
pub struct CreateTeamParams {
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: CreateTeam,
}

impl CreateTeamParams {
    /// Construct a new `CreateTeamParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: CreateTeam) -> Self {
        Self { body }
    }
}

impl<'a> PlatformTeamsApi<'a> {
    /// Create a team
    ///
    /// Creates a team along with its default project, a staging environment, and a production environment. An admin invitation is sent to `admin_email`, onboarding is marked complete with AuthKit enabled, and the calling platform is authorized to act inside the team.
    pub async fn create_team(&self, params: CreateTeamParams) -> Result<Team, Error> {
        self.create_team_with_options(params, None).await
    }

    /// Variant of [`Self::create_team`] that accepts per-request [`crate::RequestOptions`].
    pub async fn create_team_with_options(
        &self,
        params: CreateTeamParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<Team, Error> {
        let path = "/platform/teams".to_string();
        let method = http::Method::POST;
        self.client
            .request_with_body_opts(method, &path, &params, Some(&params.body), options)
            .await
    }

    /// Get a team
    ///
    /// Returns a team, and doubles as a health check on the platform's access to it. Read `production_state` before attempting to create a production environment.
    pub async fn get_team(&self, team_id: &str) -> Result<Team, Error> {
        self.get_team_with_options(team_id, None).await
    }

    /// Variant of [`Self::get_team`] that accepts per-request [`crate::RequestOptions`].
    pub async fn get_team_with_options(
        &self,
        team_id: &str,
        options: Option<&crate::RequestOptions>,
    ) -> Result<Team, Error> {
        let team_id = crate::client::path_segment(team_id);
        let path = format!("/platform/teams/{team_id}");
        let method = http::Method::GET;
        self.client
            .request_with_query_opts(method, &path, &(), options)
            .await
    }
}
