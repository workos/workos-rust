// Code generated by oagen. DO NOT EDIT.

use serde::{Deserialize, Serialize};
use std::fmt;
use std::str::FromStr;

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
#[non_exhaustive]
pub enum DirectoryType {
    AzureSCIMV20,
    Bamboohr,
    BreatheHr,
    CezanneHr,
    CyberarkSCIMV20,
    FourthHr,
    GenericSCIMV20,
    GsuiteDirectory,
    Hibob,
    SailpointSCIMV20,
    JumpCloudSCIMV20,
    OktaSCIMV20,
    OneloginSCIMV20,
    PeopleHr,
    Personio,
    PingfederateSCIMV20,
    RipplingSCIMV20,
    S3,
    Sftp,
    SftpWorkday,
    Workday,
    /// Wire value not recognized by this SDK version. The original
    /// string is preserved verbatim. WorkOS may add new enum values
    /// server-side; matching on this variant lets callers handle
    /// forward-compatible values without panicking.
    Unknown(String),
}

impl DirectoryType {
    /// Canonical wire string for this value. For [`Self::Unknown`] returns the
    /// original wire value as received from the API.
    #[allow(deprecated)]
    pub fn as_str(&self) -> &str {
        match self {
            Self::AzureSCIMV20 => "azure scim v2.0",
            Self::Bamboohr => "bamboohr",
            Self::BreatheHr => "breathe hr",
            Self::CezanneHr => "cezanne hr",
            Self::CyberarkSCIMV20 => "cyberark scim v2.0",
            Self::FourthHr => "fourth hr",
            Self::GenericSCIMV20 => "generic scim v2.0",
            Self::GsuiteDirectory => "gsuite directory",
            Self::Hibob => "hibob",
            Self::SailpointSCIMV20 => "sailpoint scim v2.0",
            Self::JumpCloudSCIMV20 => "jump cloud scim v2.0",
            Self::OktaSCIMV20 => "okta scim v2.0",
            Self::OneloginSCIMV20 => "onelogin scim v2.0",
            Self::PeopleHr => "people hr",
            Self::Personio => "personio",
            Self::PingfederateSCIMV20 => "pingfederate scim v2.0",
            Self::RipplingSCIMV20 => "rippling scim v2.0",
            Self::S3 => "s3",
            Self::Sftp => "sftp",
            Self::SftpWorkday => "sftp workday",
            Self::Workday => "workday",
            Self::Unknown(s) => s.as_str(),
        }
    }
}

impl fmt::Display for DirectoryType {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        f.write_str(self.as_str())
    }
}

impl AsRef<str> for DirectoryType {
    fn as_ref(&self) -> &str {
        self.as_str()
    }
}

impl FromStr for DirectoryType {
    type Err = std::convert::Infallible;
    #[allow(deprecated)]
    fn from_str(s: &str) -> Result<Self, Self::Err> {
        Ok(match s {
            "azure scim v2.0" => Self::AzureSCIMV20,
            "bamboohr" => Self::Bamboohr,
            "breathe hr" => Self::BreatheHr,
            "cezanne hr" => Self::CezanneHr,
            "cyberark scim v2.0" => Self::CyberarkSCIMV20,
            "fourth hr" => Self::FourthHr,
            "generic scim v2.0" => Self::GenericSCIMV20,
            "gsuite directory" => Self::GsuiteDirectory,
            "hibob" => Self::Hibob,
            "sailpoint scim v2.0" => Self::SailpointSCIMV20,
            "jump cloud scim v2.0" => Self::JumpCloudSCIMV20,
            "okta scim v2.0" => Self::OktaSCIMV20,
            "onelogin scim v2.0" => Self::OneloginSCIMV20,
            "people hr" => Self::PeopleHr,
            "personio" => Self::Personio,
            "pingfederate scim v2.0" => Self::PingfederateSCIMV20,
            "rippling scim v2.0" => Self::RipplingSCIMV20,
            "s3" => Self::S3,
            "sftp" => Self::Sftp,
            "sftp workday" => Self::SftpWorkday,
            "workday" => Self::Workday,
            other => Self::Unknown(other.to_string()),
        })
    }
}

impl From<String> for DirectoryType {
    fn from(s: String) -> Self {
        // Reuse the original `String` allocation in the fallback branch.
        match Self::from_str(&s) {
            Ok(Self::Unknown(_)) => Self::Unknown(s),
            Ok(other) => other,
        }
    }
}

impl From<&str> for DirectoryType {
    fn from(s: &str) -> Self {
        Self::from_str(s).unwrap_or_else(|_| Self::Unknown(s.to_string()))
    }
}

impl Serialize for DirectoryType {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.as_str())
    }
}

impl<'de> Deserialize<'de> for DirectoryType {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}
