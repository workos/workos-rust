// This file is auto-generated by oagen. Do not edit.

use serde::{Deserialize, Serialize};
use std::fmt;
use std::str::FromStr;

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
#[non_exhaustive]
pub enum AgentBlueprintsTokenMintTokenRequestType {
    UserDelegated,
    Autonomous,
    AgentDelegated,
    Refresh,
    /// Wire value not recognized by this SDK version. The original
    /// string is preserved verbatim. WorkOS may add new enum values
    /// server-side; matching on this variant lets callers handle
    /// forward-compatible values without panicking.
    Unknown(String),
}

impl AgentBlueprintsTokenMintTokenRequestType {
    /// Canonical wire string for this value. For [`Self::Unknown`] returns the
    /// original wire value as received from the API.
    #[allow(deprecated)]
    pub fn as_str(&self) -> &str {
        match self {
            Self::UserDelegated => "user_delegated",
            Self::Autonomous => "autonomous",
            Self::AgentDelegated => "agent_delegated",
            Self::Refresh => "refresh",
            Self::Unknown(s) => s.as_str(),
        }
    }
}

impl fmt::Display for AgentBlueprintsTokenMintTokenRequestType {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        f.write_str(self.as_str())
    }
}

impl AsRef<str> for AgentBlueprintsTokenMintTokenRequestType {
    fn as_ref(&self) -> &str {
        self.as_str()
    }
}

impl FromStr for AgentBlueprintsTokenMintTokenRequestType {
    type Err = std::convert::Infallible;
    #[allow(deprecated)]
    fn from_str(s: &str) -> Result<Self, Self::Err> {
        Ok(match s {
            "user_delegated" => Self::UserDelegated,
            "autonomous" => Self::Autonomous,
            "agent_delegated" => Self::AgentDelegated,
            "refresh" => Self::Refresh,
            other => Self::Unknown(other.to_string()),
        })
    }
}

impl From<String> for AgentBlueprintsTokenMintTokenRequestType {
    fn from(s: String) -> Self {
        // Reuse the original `String` allocation in the fallback branch.
        match Self::from_str(&s) {
            Ok(Self::Unknown(_)) => Self::Unknown(s),
            Ok(other) => other,
        }
    }
}

impl From<&str> for AgentBlueprintsTokenMintTokenRequestType {
    fn from(s: &str) -> Self {
        Self::from_str(s).unwrap_or_else(|_| Self::Unknown(s.to_string()))
    }
}

impl Serialize for AgentBlueprintsTokenMintTokenRequestType {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.as_str())
    }
}

impl<'de> Deserialize<'de> for AgentBlueprintsTokenMintTokenRequestType {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}
